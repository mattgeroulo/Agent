This project implements a local, tool-augmented AI agent powered by the Google Gemini API.
The agent can autonomously inspect a project directory, read and modify files, and respond to user queries by dynamically invoking structured system tools.
