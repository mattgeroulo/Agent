{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: main.py (uv-style args)",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/main.py",
            "console": "integratedTerminal",
            "args": [
                "how does the calculator render results to the console"
            ]
        }
    ]
}